# Anthropic API key (required for analysis and mentor chat)
ANTHROPIC_API_KEY=sk-ant-...

# Optional: comma-separated absolute path prefixes allowed for scanning.
# If unset, any existing local directory on the machine may be scanned.
# ALLOWED_SCAN_ROOTS=C:\Users\Can\projects,C:\dev
